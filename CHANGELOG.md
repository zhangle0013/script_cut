# 更新日志

本文件记录 ScriptCut 面向用户的版本变更。版本号与 `package.json` 的 `version` 字段一致。

## [0.3.0] — 2026-05-09

### 时间轴 UI

- **吸附**：默认吸附阈值调整为 **0.1s**（原 0.08s）。
- **画面轨**：同一视觉轨强制 **单行叠放**（不再因时间重叠拆成多行）；拖拽与 **← / →** 键盘平移后，用 **`visualSegmentClamp`** 钳制区间，保证画面段之间 **不互相重叠**（允许端点相接）。
- **空隙与波纹删除**：区分 **全轨空白**（蓝色高亮，`Delete` 全轨波纹左移）与 **仅当前轨空白**（黄色高亮，`Delete` 只波纹该轨）。
- **播放头拆分**：工具栏 **「播放头拆分全部」** —— 无需选中，对所有被播放头穿过的片段/段落各切一刀。
- **口播延长**：拆分为 **「波纹延长」**（全局 ripple + 重算 cuts）与 **「延长（无波纹）」**（只拉长当前条出点）。
- **多选说明**：文案同步 **Ctrl/Cmd + 点击**、**Shift + 点击扩选**、**Ctrl/Cmd + Shift 并入区间** 等行为。

### 规范文档

- **`docs/SCRIPT_CUT_AI_SPEC.md`**：新增 **§5.9**，明确 **`trk_action.text`** 为节拍摘要，**禁止**与同时间段 **`visualSegments[].description`** 全文或长段复述；并修订 §5.7 / §5.8 / §六 / §7.2 / §八 / §九 等相关表述。

### 开发

- 新增模块：`src/ui/visualSegmentClamp.ts`（画面段时间区间重叠判定与钳制）。

---

## [0.2.0] — 先前版本（摘要）

- 环境轨、时间轴交互与工程收尾（详见 `git log`）。

## [0.1.0]

- 初始开源：CLI 解析、时间轴编辑器 MVP。
